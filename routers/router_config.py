from fastapi import APIRouter

router = APIRouter()

#router_login = APIRouter(prefix='/login',tags=['Login'])
#router_email = APIRouter(prefix='/insert_emails',tags=['Insert'])
#router_bup = APIRouter(prefix='/backup',tags=['Backup'])